<!DOCTYPE html>
<html>
  <head>
    <title>{{film.title}}</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        margin: 0;
        padding: 20px;
      }
      
      h1 {
        color: #333;
        margin-bottom: 10px;
      }
      
      img {
        max-width: 100%;
        height: auto;
        margin-bottom: 20px;
      }
      
      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #fff;
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }
    </style>
  </head>
  <body> 
    <div class="container">
      <h1>{{film.title}}</h1>
      <h2>{{film.year}}</h2>
      <h3>{{film.director}}</h3>
      <h3>{{film.genre}}</h3>
      <img src="/public/{{ film.image }}" alt="Film Image">
    </div>
  </body>
</html>
